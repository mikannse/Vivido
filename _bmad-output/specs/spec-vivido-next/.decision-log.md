# Decision Log — spec-vivido-next

## 2026-07-07 · Create（首次蒸馏）

**输入源**：`_bmad-output/epics.md`（4 Epic / 10 Story / 9 FR / 7 NFR / 8 AR，status: complete）+ `_bmad-output/architecture.md`（4 ADR / 命名·结构·过程约定 / 7 条强制规则 / 文件落点映射 / 语音边界 E1·E3·E4·E5，status: complete）。二者结构化且已对齐，属上游 BMad 产物。

**关键决策**：
- 范围 = 整个下一阶段路线图（用户确认）；slug = `vivido-next`（呼应记忆 vivido-next-roadmap）。
- `epics.md` 与 `architecture.md` 均列为 **adopted companions**（非 sources）——两者对下游实现仍有必读价值（Story 级 GWT 验收标准、ADR 证据、强制规则、文件映射），故引用而不复制、不编辑；`sources: []`。
- 9 个 FR → CAP-1..9 一一映射，保持 epics 的 FR 顺序。
- NFR/AR/5 条产品原则 → 蒸馏为 Why + Constraints + Non-goals，不复制表格。

**Pass 1 — Coherence（Spec Law 1-6, 8）**：9 个能力均具 intent + testable success ✓。CAP-8/9 的 success 含机制名（saveMedia/BACKUP_VERSION/白名单）——这些是 ADR 拍定的 load-bearing 契约点，作为可判定标准保留，intent 本身仍描述 WHAT ✓。12 条 Constraints 均真实约束设计（无 AI/零依赖/无全局态/无 loading/备份白名单陷阱/iOS 权限等）✓。Non-goals 6 项、Success signal 为可端到端演示的记忆闭环 ✓。CAP IDs 唯一稳定 ✓。散文精简 ✓。两处未拍板项下沉为 Open Questions（词云是否纳入词典增强、CarouselCard 新建 vs 复用），未虚构内容。

**Pass 2 — Preservation**：逐条走查——9 FR 全部落 CAP；NFR1-7 落 Constraints/Non-goals；AR1-8 落 Constraints（偏好基元/语音契约/schema/白名单/iOS/词云/既有约定/主题债）+ companions；语音 4 个边界防护落 CAP-8 success + architecture companion；5 条产品原则落 Why + Constraints。未发现 load-bearing 源声明丢失。Wrapper-only 内容（epics/architecture 的 frontmatter stepsCompleted、验证清单勾选、README 式过程叙述）按约定不搬入内核。

**Verdict**：9 能力 / 0 spec-authored companion / 2 adopted companion（architecture.md, epics.md）。契约完整、可判定、无 Critical 缺口；2 个 Open Question 待人确认，均不阻塞下游起步（Epic 1/2 零 ADR 依赖可先行）。

## 2026-07-07 · Update（Open Questions 裁定）

用户过审两个 Open Question，均按推荐拍板：
- **CAP-4 词云算法** → 本期仅零依赖规则（停用词 + 词频/字长启发式）；ADR-4 本地词典方案确定为后续可选增强，不入本期。已更新 CAP-4 success 显式排除词典，并记入 Assumptions。
- **CAP-6 轮播卡片** → 新建独立 `CarouselCard.tsx`（大图单焦点），与 `TimelineCard` 分离，不复用同一组件。已更新 CAP-6 success 指明落点，并记入 Assumptions。

Open Questions 段清空后移除；两项决定下沉为 Assumptions。SPEC 契约无其它变动，能力数仍为 9。待同步至源文档 `architecture.md`（ADR-4 词典标注为「本期不做」）与 `epics.md`（Story 1.3 / Story 3.1 备注）——如用户确认更新源。

**源文档已同步（2026-07-07）**：用户确认更新源。
- `architecture.md`：ADR-4「可选增强」行标注本期仅零依赖规则；文件树 `CarouselCard.tsx` 由「可选」改为 🟢 新建（独立组件）；需求→文件映射与「新增文件」小结同步（5 个新增文件）。
- `epics.md`：Story 1.3 末尾加词云决策备注；Story 3.1 轮播卡片 AC 加 CarouselCard 独立组件决策备注。
三处（SPEC / architecture / epics）现已一致。
